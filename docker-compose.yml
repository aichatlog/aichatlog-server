services:
  aichatlog-server:
    image: ghcr.io/aichatlog/aichatlog/server:latest
    ports:
      - "4180:4180"
    volumes:
      - ./data:/app/data
    environment:
      - AICHATLOG_TOKEN=${AICHATLOG_TOKEN:-}
    restart: unless-stopped
